# 实验步骤（特别详细版）

## 一、实验目标
1. 完成 JSP + Servlet + JavaBean + MySQL 学生信息管理系统的部署与运行。
2. 验证系统核心业务功能：登录权限、学生/教师/班级/课程/成绩管理。
3. 完成页面视觉优化，使系统界面更清晰、美观、可用。
4. 形成完整实验交付：代码、WBS 分工、README、详细实验文档、版本提交。

## 二、实验环境准备（逐项）
1. 操作系统
- Windows 10/11（本实验环境为 Windows）

2. 软件版本建议
- JDK：1.8
- MySQL：5.7 或 8.0
- Tomcat：8.x/9.x
- 开发工具：IDEA/Eclipse（支持 Java Web）
- 浏览器：Chrome/Edge

3. 必检环境变量
1. `JAVA_HOME` 指向 JDK 安装目录
2. `Path` 中包含 `%JAVA_HOME%/bin`
3. 在终端执行 `java -version` 返回 1.8.x

## 三、项目导入与结构确认
1. 获取项目目录
- 项目根目录下包含 Student 文件夹与数据库脚本 student.sql。

2. 导入方式
1. 打开 IDEA/Eclipse
2. 选择导入已有 Web 项目（Dynamic Web Project / Java Web）
3. 将 Student 识别为 Web 应用
4. 配置 Tomcat 服务器

3. 导入后结构核对
1. Student/src：Java 代码
2. Student/WebRoot：前端页面与资源
3. Student/WebRoot/WEB-INF/web.xml：Filter 与 Servlet 映射
4. Student/student.sql：数据库初始化脚本

## 四、数据库初始化（超详细）
1. 创建数据库
1. 打开 MySQL 客户端或可视化工具
2. 新建数据库 `student`，字符集建议 `utf8` 或 `utf8mb4`

2. 执行脚本
1. 打开 Student/student.sql
2. 全量执行 SQL
3. 若出现权限错误，确认当前账号具备创建表/触发器权限

3. 核验数据
1. 执行 `show tables;`，检查是否存在 `student`、`teacher`、`classes`、`subject`、`score`、`operator`、`privilege` 等
2. 随机查询 2~3 张表，确认有初始数据
3. 重点核验 operator 表，确认存在 `admin/admin` 账户

## 五、后端配置与部署
1. 数据库连接检查
1. 打开 `Student/src/util/DB.java`
2. 核对连接串、用户名、密码
3. 若本地端口非 3306，按实际环境修改

2. Web 配置检查
1. 打开 `Student/WebRoot/WEB-INF/web.xml`
2. 核对 welcome-file 为 `login.jsp`
3. 检查 EnCodeFilter 与 LoginFilter 是否映射正确
4. 检查关键 Servlet 映射是否完整（LoginServlet、SearchStudentServlet、SearchScoreServlet 等）

3. 部署到 Tomcat
1. 将 Student 发布到 Tomcat webapps
2. 启动 Tomcat
3. 观察控制台日志，确认无严重启动异常

## 六、系统启动与功能验证
1. 访问系统
1. 浏览器访问 `http://localhost:8080/Student/`
2. 进入登录页

2. 登录验证
1. 使用 `admin/admin` 登录
2. 验证进入 `pages/index.jsp` 主框架页
3. 验证顶部显示用户信息，左侧显示菜单

3. 权限验证（建议三类角色都测）
1. 管理员：应可见完整管理菜单
2. 教师：应可见教师相关与部分查询菜单
3. 学生：应可见个人资料与成绩查询等菜单

4. 业务链路验证（建议按顺序）
1. 专业管理：新增专业 -> 查询 -> 修改 -> 删除（可选）
2. 班级管理：新增班级 -> 查询 -> 修改 -> 删除（可选）
3. 学生管理：新增学生 -> 查询 -> 修改 -> 删除（可选）
4. 教师管理：新增教师 -> 查询 -> 修改 -> 删除（可选）
5. 课程管理：新增课程 -> 查询 -> 修改 -> 删除（可选）
6. 班级课程：添加班级课程关联 -> 查询课程表 -> 导出
7. 成绩管理：查询成绩 -> 修改成绩 -> 再次查询确认

5. 异常与边界验证
1. 输入空值时是否提示
2. 重复主键/唯一值时是否拦截
3. 未登录直接访问 pages 路径是否被拦截到登录逻辑
4. 注销后刷新主页面是否需要重新登录

## 七、界面优化实施步骤（本次已完成）
1. 登录页优化
1. 文件：Student/WebRoot/login.jsp、Student/WebRoot/css/style.css
2. 改动：
- 登录卡片化
- 输入框焦点态
- 登录按钮渐变与悬停反馈
- 背景改为多层渐变，不再是单一平铺纹理

2. 顶栏优化
1. 文件：Student/WebRoot/css/top.css
2. 改动：
- 顶栏采用品牌化渐变
- 用户操作区改为胶囊风格
- 链接颜色和 hover 态更清晰

3. 左侧菜单优化
1. 文件：Student/WebRoot/css/left.css
2. 改动：
- 菜单分层更明显
- 一级菜单增加高亮边条
- 二级菜单背景与层级对比增强

4. 欢迎页优化
1. 文件：Student/WebRoot/pages/welcome.jsp
2. 改动：
- 欢迎信息卡片化
- 标题、分割线、正文层次重排
- 文本可读性提升

## 八、文档编写步骤
1. README.md
1. 写明项目简介、技术栈、运行方式、验收建议
2. 记录本次优化点与后续改进建议

2. WBS.md
1. 列出任务分解（WBS）
2. 明确四位成员分工
3. 增加计划耗时与实际耗时对比表

3. 实验步骤.md
1. 以“可复现”为目标写明完整流程
2. 每个阶段包含检查点与常见问题排查

## 九、Git 版本管理与提交步骤
1. 初始化仓库（若尚未初始化）
1. 在项目根目录执行 `git init`
2. 创建 `.gitignore`（可选）

2. 提交代码
1. 执行 `git add .`
2. 执行 `git commit -m "feat: 完成实验七文档与界面优化"`

3. 绑定远程仓库并推送
1. 执行 `git remote add origin <你的仓库地址>`
2. 执行 `git branch -M main`
3. 执行 `git push -u origin main`

4. 若推送失败（鉴权问题）
1. 检查是否已登录 GitHub
2. 使用 PAT（Personal Access Token）替代密码
3. 重新推送

## 十、验收清单（提交前逐条打勾）
1. 系统可正常启动
2. 数据库初始化成功
3. 三类角色登录与菜单权限正常
4. 核心增删改查链路至少通过一遍
5. 页面样式已完成优化且显示正常
6. README.md、WBS.md、实验步骤.md 已提交
7. Git 提交记录清晰，远程仓库可见

## 十一、常见问题与排查
1. 页面中文乱码
- 检查 JSP pageEncoding、response 编码、过滤器编码设置

2. 数据库连接失败
- 检查 DB.java 的 URL、用户名、密码
- 检查 MySQL 服务是否启动

3. 404 或 Servlet 不生效
- 检查 web.xml 的 `<url-pattern>` 映射
- 检查项目发布路径是否为 `/Student`

4. 登录后空白或跳转异常
- 检查 LoginServlet 返回值
- 检查 JS 中 window.open 路径

## 十二、实验结果说明
1. 系统已可运行，核心模块可访问。
2. 首页与导航视觉风格已统一，观感明显优于原始样式。
3. 已新增“成绩统计分析”功能（教师/管理员可见），含平均分、及格率、分布图、分科目统计。
4. 文档交付完整：README、WBS、建模图与任务对照、实验步骤。

## 十三、按任务图逐条对照
1. 任务1：已完成部署说明与三类角色操作流程。
2. 任务2：已完成用例图（见 建模图与任务对照.md）。
3. 任务3：已完成面向对象分析类图（见 建模图与任务对照.md）。
4. 任务4：已完成学生查询课程成绩顺序图（见 建模图与任务对照.md）。
5. 任务5：已完成数据库 ER 图（见 建模图与任务对照.md）。
6. 任务6：已完成 MVC 类图、类图关系总结、dao 层用途分析（见 建模图与任务对照.md）。
7. 任务7：已完成新增功能“成绩统计分析”，并已提交 GitHub。
8. 任务8：已完成 WBS（含计划耗时与实际耗时表）。

## 十四、截图提交建议
1. 登录页截图 1 张。
2. 管理员主界面截图 1 张。
3. 教师主界面截图 1 张。
4. 学生主界面截图 1 张。
5. 新增“成绩统计分析”页面截图 1 张。
