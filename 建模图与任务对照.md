# 实验七建模图与任务对照

## 一、任务2：高校教学管理软件用例图（20分）
图名：UTMS-UseCase

```mermaid
flowchart LR
    A[学生] --> U1[查看课程]
    A --> U2[查询成绩]
    A --> U3[查看同班同学]

    B[教师] --> U4[查看课表]
    B --> U5[查询学生成绩]
    B --> U6[录入/修改成绩]
    B --> U7[导出班级成绩]
    B --> U8[成绩统计分析]

    C[教务员/管理员] --> U9[维护学生信息]
    C --> U10[维护教师信息]
    C --> U11[维护课程信息]
    C --> U12[维护专业与班级]
    C --> U13[班级课程排课]
    C --> U14[权限与用户管理]

    D[系统] --> U15[登录认证]
    D --> U16[权限过滤]
```

## 二、任务3：面向对象分析类图（20分）
图名：UTMS-OOA-Class

```mermaid
classDiagram
    class Operator{
      +Integer id
      +String name
      +String pwd
      +Role role
    }
    class Role{
      +Integer id
      +String name
    }
    class Privilege{
      +Integer id
      +String name
      +String url
      +String menuName
    }
    class Student{
      +Integer id
      +String no
      +String name
      +String sex
      +String phone
      +Classes classes
    }
    class Teacher{
      +Integer id
      +String name
      +String sex
      +String major
      +String phone
      +Operator operator
    }
    class Classes{
      +Integer id
      +String name
      +String claTec
      +Major major
    }
    class Major{
      +Integer id
      +String name
      +String principal
      +String phone
    }
    class Subject{
      +Integer id
      +String name
      +Double score
      +String time
    }
    class Cla2Sub{
      +Integer id
      +Classes classes
      +Subject subject
      +Teacher teacher
    }
    class Score{
      +Integer id
      +Double daily
      +Double exam
      +Double count
      +Student student
      +Subject subject
    }

    Role "1" <-- "*" Operator
    Operator "1" --> "1" Student
    Operator "1" --> "1" Teacher
    Role "1" --> "*" Privilege
    Major "1" --> "*" Classes
    Classes "1" --> "*" Student
    Classes "1" --> "*" Cla2Sub
    Subject "1" --> "*" Cla2Sub
    Teacher "1" --> "*" Cla2Sub
    Student "1" --> "*" Score
    Subject "1" --> "*" Score
```

## 三、任务4：学生查询课程和成绩顺序图（20分）
图名：UTMS-StudentScore-Sequence

```mermaid
sequenceDiagram
    participant S as Student
    participant JSP as search_score.jsp
    participant JS as search_score.js
    participant SS as SearchScoreServlet
    participant SI as ScoreImpl
    participant DB as MySQL

    S->>JSP: 进入成绩查询页面
    S->>JS: 选择条件并点击查询
    JS->>SS: POST /SearchScoreServlet(search_type,value,page)
    SS->>SI: query(type,value,page)
    SI->>DB: 执行SQL并返回成绩集合
    DB-->>SI: ResultSet
    SI-->>SS: List<Score>
    SS-->>JS: JSON数据
    JS-->>JSP: 渲染成绩表格
    S->>JSP: 查看成绩结果
```

## 四、任务5：数据库ER图（20分）
图名：UTMS-ER

```mermaid
erDiagram
    ROLE ||--o{ OPERATOR : has
    ROLE ||--o{ PRIVILEGE : grants
    OPERATOR ||--|| STUDENT : maps
    OPERATOR ||--|| TEACHER : maps
    MAJOR ||--o{ CLASSES : contains
    CLASSES ||--o{ STUDENT : has
    CLASSES ||--o{ CLA2SUB : arranges
    SUBJECT ||--o{ CLA2SUB : arranged
    TEACHER ||--o{ CLA2SUB : teaches
    STUDENT ||--o{ SCORE : owns
    SUBJECT ||--o{ SCORE : evaluates

    ROLE {
      int rol_id PK
      varchar rol_name
    }
    OPERATOR {
      int ope_id PK
      varchar ope_name
      varchar ope_pwd
      int rol_id FK
    }
    PRIVILEGE {
      int pri_id PK
      varchar pri_name
      varchar pri_url
      varchar menu_name
      int rol_id FK
    }
    MAJOR {
      int maj_id PK
      varchar maj_name
      varchar maj_prin
      varchar maj_phone
    }
    CLASSES {
      int cla_id PK
      varchar cla_name
      int maj_id FK
      varchar cla_tec
    }
    STUDENT {
      int stu_id PK
      int ope_id FK
      varchar stu_no
      varchar stu_name
      int cla_id FK
    }
    TEACHER {
      int tec_id PK
      int ope_id FK
      varchar tec_name
      varchar tec_major
    }
    SUBJECT {
      int sub_id PK
      varchar sub_name
      double sub_score
    }
    CLA2SUB {
      int cla2sub_id PK
      int cla_id FK
      int sub_id FK
      int tec_id FK
    }
    SCORE {
      int sco_id PK
      double sco_daily
      double sco_exam
      double sco_count
      int stu_id FK
      int sub_id FK
      int cla2sub_id FK
      int cla_id FK
    }
```

## 五、任务6：结合MVC的软件类图与代码结构说明（40+5+5分）
图名：UTMS-MVC-Class

```mermaid
classDiagram
    class View{
      +JSP pages
      +JS/CSS
    }
    class Controller{
      +LoginServlet
      +SearchScoreServlet
      +ScoreStatsServlet
      +AddStudentServlet
      +...其他Servlet
    }
    class ModelEntity{
      +Student
      +Teacher
      +Classes
      +Subject
      +Score
      +...
    }
    class ModelDAO{
      +IStudent/StudentImpl
      +ITeacher/TeacherImpl
      +IScore/ScoreImpl
      +...
    }
    class DBUtil{
      +DB.getConn()
      +DB.close()
    }

    View --> Controller : HTTP/AJAX
    Controller --> ModelDAO : 调用业务查询
    ModelDAO --> DBUtil : 获取连接
    ModelDAO --> ModelEntity : 组装实体
    Controller --> View : 转发/JSON响应
```

### 任务6-2：任务3与任务6类图关系总结（5分）
1. 任务3偏业务领域对象抽象（实体关系与业务属性）。
2. 任务6偏实现架构分层（View-Controller-Model-DB）。
3. 两者关系：任务3给出“是什么”，任务6给出“怎么实现”。

### 任务6-3：DAO包（题目中“fda包”按代码结构理解为dao层）用途分析（5分）
1. IStudent、ITeacher、IScore 等接口定义数据访问契约。
2. impl 包中的 StudentImpl、TeacherImpl、ScoreImpl 是接口实现。
3. 控制器 Servlet 通过 impl 访问数据库，避免页面直接操作 SQL。
4. dao/impl 分离便于后续替换数据源或重构业务逻辑。

## 六、任务7：新增功能说明（30+2分）
新增功能：教师/管理员成绩统计分析

已实现内容：
1. 后端接口：/Student/ScoreStatsServlet
2. 前端页面：pages/score_stats.jsp
3. 脚本样式：js/score_stats.js + css/score_stats.css
4. 页面入口：search_score.jsp 新增“成绩统计分析”链接（教师/管理员可见）
5. 统计指标：平均分、及格人数、不及格人数、及格率、分段分布图、分科目统计表

## 七、任务8：WBS
见根目录 [WBS.md](WBS.md)

## 八、任务完成性检查表
- [x] 任务1：部署与三类用户登录操作（代码和步骤文档已覆盖，截图需运行环境手动截取）
- [x] 任务2：用例图（本文件）
- [x] 任务3：面向对象分析类图（本文件）
- [x] 任务4：顺序图（本文件）
- [x] 任务5：ER图（本文件）
- [x] 任务6：MVC类图 + 图关系总结 + dao用途分析（本文件）
- [x] 任务7：新增可运行功能 + GitHub源码提交
- [x] 任务8：WBS（含计划/实际耗时）
